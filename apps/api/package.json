{
  "name": "@flowguard/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "predev": "npm run build --workspace @flowguard/recovery",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/cors": "latest",
    "@flowguard/domain": "0.1.0",
    "@flowguard/recovery": "^0.1.0",
    "fastify": "latest",
    "zod": "latest"
  }
}
